import React from 'react';
import { Container } from '../../components/Container';
import { MainWrapper } from '../../components/MainWrapper';

const About: React.FC = () => {
    return (
        <MainWrapper>
            <Container fullheight>Página</Container>
        </MainWrapper>
    );
};

export default About;
